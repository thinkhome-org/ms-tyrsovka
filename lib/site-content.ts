/**
 * Editable site copy (page titles, nav labels, etc.).
 * Single source for the Zápisy page and its nav link.
 */

export const SITE_PAGES = {
    zapisy: {
        /** Main heading on /zapisy */
        title: "Zápisy",
        /** Label in the navbar (Informace → Zápisy) */
        navLabel: "Zápisy",
    },
} as const;
